# 📱 Инструкция: Сборка Android приложения StreamBoom

## 🎯 Что нужно установить

### 1. Node.js
- Скачай с https://nodejs.org/
- Выбери LTS версию (рекомендуемая)
- Установи с настройками по умолчанию

### 2. Android Studio
- Скачай с https://developer.android.com/studio
- Установи с настройками по умолчанию
- При первом запуске выбери "Standard Installation"
- Дождись окончания загрузки компонентов

### 3. Java Development Kit (JDK)
- Android Studio устанавливает JDK автоматически
- Если нужно отдельно: https://www.oracle.com/java/technologies/downloads/

---

## 🚀 Пошаговая сборка .apk

### Шаг 1: Скачай проект с GitHub

1. Зайди в редактор poehali.dev
2. Нажми **"Скачать → Подключить GitHub"**
3. Авторизуйся в GitHub
4. Скачай репозиторий на компьютер:
   ```bash
   git clone https://github.com/ТВОЙ-АККАУНТ/ИМЯ-РЕПОЗИТОРИЯ.git
   cd ИМЯ-РЕПОЗИТОРИЯ
   ```

### Шаг 2: Установи зависимости

```bash
npm install
```

или если используешь Bun:

```bash
bun install
```

### Шаг 3: Собери веб-версию

```bash
npm run build
```

Это создаст папку `dist/` с готовой веб-версией.

### Шаг 4: Добавь Android платформу

```bash
npx cap add android
```

Capacitor создаст папку `android/` с проектом Android Studio.

### Шаг 5: Синхронизируй изменения

```bash
npx cap sync android
```

Это скопирует файлы из `dist/` в Android проект.

### Шаг 6: Открой проект в Android Studio

```bash
npx cap open android
```

Или открой вручную:
1. Запусти Android Studio
2. File → Open
3. Выбери папку `android/`

### Шаг 7: Собери APK

#### Вариант A: Debug APK (для тестирования)

1. В Android Studio нажми **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. Дождись окончания сборки
3. Нажми "locate" в уведомлении
4. APK находится в `android/app/build/outputs/apk/debug/app-debug.apk`

#### Вариант B: Release APK (для публикации)

1. **Создай keystore** (один раз):
   ```bash
   keytool -genkey -v -keystore streamboom-release.keystore -alias streamboom -keyalg RSA -keysize 2048 -validity 10000
   ```
   
   Сохрани пароли! Они понадобятся.

2. **Настрой подпись** в `android/app/build.gradle`:
   ```gradle
   android {
       signingConfigs {
           release {
               storeFile file('../../streamboom-release.keystore')
               storePassword 'ТВОЙ_ПАРОЛЬ_STORE'
               keyAlias 'streamboom'
               keyPassword 'ТВОЙ_ПАРОЛЬ_KEY'
           }
       }
       buildTypes {
           release {
               signingConfig signingConfigs.release
           }
       }
   }
   ```

3. **Собери Release APK**:
   - Build → Generate Signed Bundle / APK
   - Выбери APK
   - Укажи путь к keystore и пароли
   - Выбери "release"
   - Готово! APK в `android/app/release/app-release.apk`

---

## 📲 Установка APK на телефон

### Способ 1: USB кабель

1. Включи **"Режим разработчика"** на Android:
   - Настройки → О телефоне
   - Нажми 7 раз на "Номер сборки"
   
2. Включи **"Отладка по USB"**:
   - Настройки → Для разработчиков
   - Включи "Отладка по USB"

3. Подключи телефон к компьютеру

4. В Android Studio нажми ▶️ (Run)

### Способ 2: Файловый менеджер

1. Скопируй `.apk` файл на телефон
2. Открой файловый менеджер
3. Нажми на `.apk`
4. Разреши установку из неизвестных источников
5. Нажми "Установить"

---

## 🔄 Обновление после изменений

После изменения кода в poehali.dev:

```bash
# 1. Скачай обновления
git pull

# 2. Пересобери веб-версию
npm run build

# 3. Синхронизируй с Android
npx cap sync android

# 4. Открой в Android Studio и собери новый APK
npx cap open android
```

---

## ⚙️ Полезные команды

```bash
# Показать версию Capacitor
npx cap --version

# Обновить Capacitor
npm install @capacitor/core @capacitor/cli @capacitor/android

# Проверить конфигурацию
npx cap doctor

# Логи Android
npx cap run android --livereload

# Удалить Android платформу
rm -rf android
npx cap add android
```

---

## 🐛 Частые проблемы

### "SDK location not found"
1. Открой Android Studio
2. Tools → SDK Manager
3. Скопируй путь к Android SDK
4. Создай файл `android/local.properties`:
   ```
   sdk.dir=ПУТЬ_К_ANDROID_SDK
   ```

### "Command failed: gradlew"
```bash
cd android
./gradlew clean
cd ..
npx cap sync android
```

### "Unable to find bundled Java"
1. В Android Studio: File → Project Structure
2. SDK Location → JDK Location
3. Укажи путь к JDK

### Приложение не запускается
1. Проверь `capacitor.config.ts`
2. Убедись что `webDir: 'dist'` правильный
3. Запусти `npm run build && npx cap sync`

---

## 📦 Публикация в Google Play

1. Зарегистрируйся на https://play.google.com/console
2. Заплати $25 (один раз)
3. Создай приложение
4. Загрузи **подписанный APK** (release версию)
5. Заполни описание, скриншоты, иконки
6. Отправь на модерацию
7. Через 1-7 дней приложение появится в Google Play

---

## 🎨 Изменение иконки и названия

### Иконка
1. Замени `resources/icon.png` (размер 1024x1024)
2. Запусти: `npx cap sync android`

### Название
В `capacitor.config.ts`:
```typescript
appName: 'Твоё Название',
```

В `android/app/src/main/res/values/strings.xml`:
```xml
<string name="app_name">Твоё Название</string>
```

---

## ✅ Чеклист перед сборкой

- [ ] `npm run build` выполняется без ошибок
- [ ] `capacitor.config.ts` настроен правильно
- [ ] Android Studio установлена
- [ ] JDK настроен
- [ ] Android SDK загружен
- [ ] Иконка приложения заменена
- [ ] Название приложения изменено
- [ ] `appId` уникальный (для публикации)

---

## 📞 Поддержка

Если что-то не получается:
- Телеграм сообщество: https://t.me/+QgiLIa1gFRY4Y2Iy
- GitHub Issues: в твоём репозитории

---

**Удачи с запуском твоего приложения! 🚀**
